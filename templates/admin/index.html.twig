{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <div class="admin">
        <div>
            <h2>Langages</h2>
            {% for language in languages %}
                <ul>
                    <li>{{ language.name }}<i class="{{ language.icon }}"></i>
                        <button class="edit-btn"><a href="{{ path('admin_edit_language', {'id': language.id}) }}">Editer</a></button>
                        {% include '/component/_delete_language.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-language">
                    <button type="button" class="language-button" data-toggle="modal" data-target="#modalLanguage">
                        Créer un nouveau langage
                    </button>
                    <div class="modal fade" id="modalLanguage" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newLanguage.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Librairies</h2>
            {% for librairie in librairies %}
                <ul>
                    <li>{{ librairie.name }}<i class="{{ librairie.icon }}"></i>
                        <button class="edit-btn"><a href="{{ path('admin_edit_librairie', {'id': librairie.id}) }}">Editer</a></button>
                        {% include '/component/_delete_librairie.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-librairie">
                    <button type="button" class="language-button" data-toggle="modal" data-target="#modallibrairie">
                        Créer une nouvelle librairie
                    </button>
                    <div class="modal fade" id="modalLibrairie" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newLibrairie.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Frameworks</h2>
            {% for framework in frameworks %}
                <ul>
                    <li>{{ framework.name }}<i class="{{ framework.icon }}"></i>
                        <button class="edit-btn"><a href="{{ path('admin_edit_framework', {'id': framework.id}) }}">Editer</a></button>
                        {% include '/component/_delete_framework.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-framework">
                    <button type="button" class="framework-button" data-toggle="modal" data-target="#modalFramework">
                        Créer un nouveau framework
                    </button>
                    <div class="modal fade" id="modalFramework" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newFramework.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Design Pattern</h2>
            {% for designPattern in designPatterns %}
                <ul>
                    <li>{{ designPattern.name }}
                        <button class="edit-btn"><a href="{{ path('admin_edit_designPattern', {'id': designPattern.id}) }}">Editer</a></button>
                        {% include '/component/_delete_designPattern.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-designPattern">
                    <button type="button" class="designPattern-button" data-toggle="modal" data-target="#modalDesignPattern">
                        Créer un nouveau design pattern
                    </button>
                    <div class="modal fade" id="modalDesignPattern" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newDesignPattern.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Logiciels</h2>
            {% for software in softwares %}
                <ul>
                    <li>{{ software.name }}<i class="{{ software.icon }}"></i>
                        <button class="edit-btn"><a href="{{ path('admin_edit_software', {'id': software.id}) }}">Editer</a></button>
                        {% include '/component/_delete_software.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-software">
                    <button type="button" class="software-button" data-toggle="modal" data-target="#modalSoftware">
                        Créer un nouveau logiciel
                    </button>
                    <div class="modal fade" id="modalSoftware" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newSoftware.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Methodes</h2>
            {% for method in methods %}
                <ul>
                    <li>{{ method.name }}
                        <button class="edit-btn"><a href="{{ path('admin_edit_method', {'id': method.id}) }}">Editer</a></button>
                        {% include '/component/_delete_method.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-method">
                    <button type="button" class="method-button" data-toggle="modal" data-target="#modalMethod">
                        Créer une nouvelle méthode
                    </button>
                    <div class="modal fade" id="modalMethod" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newMethod.html.twig' %}
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>CMS</h2>
            {% for contentManagementSystem in contentManagementSystems %}
                <ul>
                    <li>{{ contentManagementSystem.name }}<i class="{{ contentManagementSystem.icon }}"></i>
                        <button class="edit-btn"><a href="{{ path('admin_edit_cms', {'id': contentManagementSystem.id}) }}">Editer</a></button>
                        {% include '/component/_delete_cms.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-contentManagementSystem">
                    <button type="button" class="contentManagementsystem-button" data-toggle="modal" data-target="#modalContentManagementSystemm">
                        Créer un nouveau CMS
                    </button>
                    <div class="modal fade" id="modalContentManagementSystem" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newContentManagementSystem.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Types de Projet</h2>
            {% for typeProject in typesProject %}
                <ul>
                    <li>{{ typeProject.name }}
                        <button class="edit-btn"><a href="{{ path('admin_edit_typeProject', {'id': typeProject.id}) }}">Editer</a></button>
                        {% include '/component/_delete_typeProject.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-typeProject">
                    <button type="button" class="typeProject-button" data-toggle="modal" data-target="#modalTypeProject">
                        Créer un nouveau type de projet
                    </button>
                    <div class="modal fade" id="modalTypeProject" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newTypeProject.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Clients</h2>
            {% for customer in customers %}
                <ul>
                    <li>{{ customer.name }}
                        <button class="edit-btn"><a href="{{ path('admin_edit_customer', {'id': customer.id}) }}">Editer</a></button>
                        {% include '/component/_delete_customer.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-customer">
                    <button type="button" class="customer-button" data-toggle="modal" data-target="#modalCustomer">
                        Créer un nouveau client
                    </button>
                    <div class="modal fade" id="modalCustomer" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newCustomer.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Projets</h2>
            {% for project in projects %}
                <ul>
                    <li>{{ project.name }}
                        <button class="edit-btn"><a href="{{ path('admin_edit_project', {'id': project.id}) }}">Editer</a></button>
                        {% include '/component/_delete_project.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-project">
                    <button type="button" class="project-button" data-toggle="modal" data-target="#modalProject">
                        Créer un nouveau projet
                    </button>
                    <div class="modal fade" id="modalProject" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newProject.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div>
            <h2>Images</h2>
            {% for picture in pictures %}
                <ul>
                    <li>{{ picture.name }}
                        <button class="edit-btn"><a href="{{ path('admin_edit_picture', {'id': picture.id}) }}">Editer</a></button>
                        {% include '/component/_delete_picture.html.twig' %}
                    </li>
                </ul>
            {% endfor %}
            <div class="profile-button-container d-flex flex-wrap justify-content-center ml-2">
                <div class="form-modal-picture">
                    <button type="button" class="picture-button" data-toggle="modal" data-target="#modalPicture">
                        Créer une nouvelle image
                    </button>
                    <div class="modal fade" id="modalPicture" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                        <div class="modal-dialog modal-dialog-centered modal-lg" role="document">
                            <div class="modal-content">
                                <div class="modal-body">
                                    {% include '/component/_newPicture.html.twig' %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
