<form method="post" action="{{ path('admin_delete_cms', {'id': contentManagementSystem.id}) }}" onsubmit="return confirm('Es-tu sûre de vouloir supprimer ce CMS ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contentManagementSystem.id) }}">
    <button class="delete-btn">Supprimer</button>
</form>