<form method="post" action="{{ path('admin_delete_designPattern', {'id': designPattern.id}) }}" onsubmit="return confirm('Es-tu sûre de vouloir supprimer ce Design Pattern ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ designPattern.id) }}">
    <button class="delete-btn">Supprimer</button>
</form>