{% block header %}
    <!--Navbar-->

    <nav class="navbar navbar-expand-lg navbar-light" >
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('home') }}"><i class="fas fa-home"></i>Accueil <span class="sr-only"></span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('about') }}"><i class="fas fa-user-alt"></i>A propos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('portfolio_index') }}"><i class="fas fa-desktop"></i>Portfolio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}"><i class="fas fa-envelope"></i>Contact</a>
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('admin_index') }}"><i class="fas fa-user-cog"></i>Admin</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('app_logout') }}"><i class="fas fa-user-check"></i>Se déconnecter</a>
                        {# <a class="nav-link" href="{{ path('admin_index') }}"><i class="fas fa-user-lock"></i>Admin</a> #}
                    {% else %}
                        <a class="nav-link" href="{{ path('app_login') }}"><i class="fas fa-user-lock"></i>Se connecter</a>
                    {% endif %}
                </li>

            </ul>
        </div>
    </nav>

{% endblock %}